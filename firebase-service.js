const cfg=window.RUFE_FIREBASE_CONFIG||{};
const EVENT_ID=window.RUFE_EVENT_ID||'san-pedro-sismo-2026';
const configured=Boolean(cfg.apiKey&&cfg.projectId&&cfg.authDomain);
const roleAliases={editor:'editor',validator:'validator',validador:'validator',admin:'admin',administrador:'admin',super_admin:'super_admin','super-admin':'super_admin','super admin':'super_admin',superadmin:'super_admin','super administrador':'super_admin','administrador principal':'super_admin'};
const normalizeRole=r=>roleAliases[String(r||'').trim().toLowerCase()]||String(r||'guest').trim().toLowerCase();
const permitted=['editor','validator','admin','super_admin'];

const SPECIAL_ADMIN_UID='EWMigkOGDdeWYb04qLehDcalXyj2';
const SPECIAL_ADMIN_EMAIL='secretariadegobierno@sanpedro-valle.gov.co';
const VALID_ROLES=['editor','validator','admin','super_admin'];
const n=v=>Math.max(0,Number(v)||0);
const clean=(v,max=800)=>String(v??'').trim().slice(0,max);
const isSpecialAdminUser=u=>Boolean(u&&(u.uid===SPECIAL_ADMIN_UID||String(u.email||'').toLowerCase()===SPECIAL_ADMIN_EMAIL));
const slug=s=>String(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
const emit=(name,detail)=>window.dispatchEvent(new CustomEvent(name,{detail}));
let app,auth,db,storage,user=null,profile=null,SDK=null,ready=false,initError=null;
let unsubPublic=null,unsubDraft=null,unsubTerritories=null,unsubWorkflow=null,pendingPublic=null;

function fallbackReport(){
  const D=structuredClone(window.REPORT_DATA||{}),geo=window.TERRITORY_MAP_DATA||[];
  D.territories=(D.territories||[]).map(t=>({...t,...(geo.find(g=>g.sector===t.sector)||{})}));
  D.releases=[{id:'3-0-2026-08-18',version:'3.0',cutoffDate:'2026-08-18',publishedAt:'2026-08-18',changeNote:'Consolidado técnico inicial del portal.',kpis:structuredClone(D.kpis),housing:structuredClone(D.housing)}];
  D.updates=[{id:'3-0-2026-08-18',date:'2026-08-18',version:'3.0',title:'Consolidado técnico v3.0',note:'Corte de información del 18 de agosto de 2026.',status:'published'}];
  D.media=[];D.publication={status:'published',version:'3.0',cutoffDate:'2026-08-18',publishedAt:'2026-08-18'};return D;
}
function assertReady(){if(!ready||!db)throw new Error(initError?'Firebase no pudo conectarse. Revisa la red e intenta nuevamente.':'Firebase todavía se está conectando. Intenta en unos segundos.');}
function hasRole(...roles){return profile?.active===true&&roles.includes(normalizeRole(profile?.role))}
function requireRole(...roles){if(!user)throw new Error('Debes iniciar sesión.');if(!hasRole(...roles))throw new Error('No tienes permisos para esta operación.');}

async function loadSdk(){
  const V='12.16.0',base=`https://www.gstatic.com/firebasejs/${V}/`;
  const [appM,authM,fsM,storageM]=await Promise.all([
    import(base+'firebase-app.js'),import(base+'firebase-auth.js'),import(base+'firebase-firestore.js'),import(base+'firebase-storage.js')
  ]);return {...appM,...authM,...fsM,...storageM};
}
async function getProfile(u){if(!u||!db)return null;const a=await SDK.getDoc(SDK.doc(db,'users',u.uid));if(a.exists())return {uid:u.uid,...a.data(),role:normalizeRole(a.data().role)};if(u.email){const email=String(u.email).toLowerCase();const b=await SDK.getDoc(SDK.doc(db,'users',email));if(b.exists())return {uid:u.uid,legacyId:email,...b.data(),role:normalizeRole(b.data().role)}}if(isSpecialAdminUser(u)){const special={uid:u.uid,email:String(u.email||SPECIAL_ADMIN_EMAIL).toLowerCase(),displayName:u.displayName||'Secretaría de Gobierno',role:'admin',active:true,rufeAccess:true,specialAdmin:true};try{await SDK.setDoc(SDK.doc(db,'users',u.uid),{...special,updatedAt:SDK.serverTimestamp(),createdAt:SDK.serverTimestamp()},{merge:true})}catch(e){console.warn('No se pudo crear perfil automático de Secretaría de Gobierno:',e)}return special}return {uid:u.uid,email:u.email||'',displayName:u.displayName||'',role:'guest',active:true}}

async function init(){
  if(!configured){initError='missing-config';emit('rufe:status',{ready:false,configured:false});return false}
  try{
    SDK=await loadSdk();app=SDK.initializeApp(cfg);auth=SDK.getAuth(app);db=SDK.getFirestore(app);storage=SDK.getStorage(app);ready=true;
    SDK.onAuthStateChanged(auth,async u=>{user=u||null;profile=u?await getProfile(u):null;emit('rufe:auth',{user,profile})});
    emit('rufe:status',{ready:true,configured:true});if(pendingPublic)attachPublic(pendingPublic);return true;
  }catch(e){initError=e;console.warn('RUFE Firebase no disponible; el portal público continúa con la copia local.',e);emit('rufe:status',{ready:false,configured:true,error:e});return false}
}
async function waitReady(){if(ready)return true;for(let i=0;i<40&&!ready&&!initError;i++)await new Promise(r=>setTimeout(r,100));assertReady();return true}
async function loginGoogle(){await waitReady();return SDK.signInWithPopup(auth,new SDK.GoogleAuthProvider())}
async function loginEmail(email,password){await waitReady();return SDK.signInWithEmailAndPassword(auth,email,password)}
async function resetPassword(email){await waitReady();return SDK.sendPasswordResetEmail(auth,email)}
async function logout(){if(auth)await SDK.signOut(auth)}

function attachPublic(callback){if(!ready||!db)return;unsubPublic?.();unsubPublic=SDK.onSnapshot(SDK.doc(db,'rufePublished',EVENT_ID),s=>{if(s.exists())callback({...fallbackReport(),...s.data()},{source:'firebase'});else callback(fallbackReport(),{source:'fallback'})},()=>callback(fallbackReport(),{source:'fallback'}))}
function listenPublic(callback){pendingPublic=callback;callback(fallbackReport(),{source:'fallback'});if(ready)attachPublic(callback);return ()=>unsubPublic?.()}
async function getPublicRelease(releaseId){await waitReady();const id=String(releaseId||'').trim();if(!id)throw new Error('Corte histórico no especificado.');const snap=await SDK.getDoc(SDK.doc(db,'rufePublished',EVENT_ID,'releases',id));return snap.exists()?{...fallbackReport(),...snap.data()}:null}
function listenDraft(callback){assertReady();requireRole('editor','validator','admin','super_admin');unsubDraft?.();unsubDraft=SDK.onSnapshot(SDK.doc(db,'rufeEvents',EVENT_ID,'drafts','current'),s=>callback(s.exists()?s.data():null));return unsubDraft}
function listenTerritories(callback){assertReady();requireRole('editor','validator','admin','super_admin');unsubTerritories?.();unsubTerritories=SDK.onSnapshot(SDK.collection(db,'rufeEvents',EVENT_ID,'territories'),s=>callback(s.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>String(a.sector).localeCompare(String(b.sector),'es'))));return unsubTerritories}
function listenWorkflow(callback){assertReady();requireRole('editor','validator','admin','super_admin');unsubWorkflow?.();unsubWorkflow=SDK.onSnapshot(SDK.doc(db,'rufeEvents',EVENT_ID,'workflow','state'),s=>callback(s.exists()?s.data():null));return unsubWorkflow}
function stopInternalListeners(){unsubDraft?.();unsubTerritories?.();unsubWorkflow?.();unsubDraft=null;unsubTerritories=null;unsubWorkflow=null}

async function saveTerritory(row){await waitReady();requireRole('editor','admin','super_admin');const sector=String(row.sector||'').trim();if(!sector)throw new Error('El territorio es obligatorio.');const clean={sector};['families','people','noHab','destroyed','noState','empty'].forEach(k=>clean[k]=Math.max(0,Number(row[k])||0));clean.lat=row.lat===''||row.lat==null?null:Number(row.lat);clean.lng=row.lng===''||row.lng==null?null:Number(row.lng);clean.precision=row.precision==='verified'?'verified':'reference';clean.reference=String(row.reference||'').trim();clean.source=String(row.source||'').trim();const batch=SDK.writeBatch(db);batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'territories',slug(sector)),{...clean,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'workflow','state'),{revision:SDK.increment(1),reviewStatus:'changes',approvedRevision:null,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});batch.set(SDK.doc(SDK.collection(db,'rufeEvents',EVENT_ID,'editorActivity')),{action:'territory.update',sector,uid:user.uid,at:SDK.serverTimestamp()});await batch.commit();return clean}
async function saveTerritoriesBulk(rows){await waitReady();requireRole('editor','admin','super_admin');if(!Array.isArray(rows)||!rows.length)throw new Error('No hay filas para importar.');if(rows.length>100)throw new Error('Máximo 100 territorios por importación.');const batch=SDK.writeBatch(db);let count=0;for(const row of rows){const sector=String(row.sector||'').trim();if(!sector)continue;const clean={sector};['families','people','noHab','destroyed','noState','empty'].forEach(k=>clean[k]=Math.max(0,Number(row[k])||0));clean.lat=row.lat===''||row.lat==null?null:Number(row.lat);clean.lng=row.lng===''||row.lng==null?null:Number(row.lng);clean.precision=row.precision==='verified'?'verified':'reference';clean.reference=String(row.reference||'').trim();clean.source=String(row.source||'').trim();batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'territories',slug(sector)),{...clean,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});count++}batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'workflow','state'),{revision:SDK.increment(1),reviewStatus:'changes',approvedRevision:null,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});batch.set(SDK.doc(SDK.collection(db,'rufeEvents',EVENT_ID,'editorActivity')),{action:'territory.bulk_import',count,uid:user.uid,at:SDK.serverTimestamp()});await batch.commit();return {count}}
async function saveDraft(patch){await waitReady();requireRole('editor','admin','super_admin');const allowed=['draftVersion','draftCutoff','meta','documentControl','executiveSummary','objective','scope','limitations','eventContext','regulatoryFramework','rufeRud','kpis','completeness','componentStatus','housing','housingInterpretation','priorityHousingNarrative','sexDistribution','lifeCycle','locationDeclared','territorialReading','qualityIssues','qualityAssuranceRules','methodology','appliedRules','priorities','prioritizationMatrix','conclusions','annexIndicators','changeNoteDraft'];const clean={};allowed.forEach(k=>{if(patch[k]!==undefined)clean[k]=patch[k]});const batch=SDK.writeBatch(db);batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'drafts','current'),{...clean,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'workflow','state'),{revision:SDK.increment(1),reviewStatus:'changes',approvedRevision:null,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});batch.set(SDK.doc(SDK.collection(db,'rufeEvents',EVENT_ID,'editorActivity')),{action:'draft.update',uid:user.uid,at:SDK.serverTimestamp()});await batch.commit()}
function initialDraft(){const D=window.REPORT_DATA;return {meta:D.meta,documentControl:D.documentControl,executiveSummary:D.executiveSummary,objective:D.objective,scope:D.scope,limitations:D.limitations,eventContext:D.eventContext,regulatoryFramework:D.regulatoryFramework,rufeRud:D.rufeRud,kpis:D.kpis,completeness:D.completeness,componentStatus:D.componentStatus,housing:D.housing,housingInterpretation:D.housingInterpretation,priorityHousingNarrative:D.priorityHousingNarrative,sexDistribution:D.sexDistribution,lifeCycle:D.lifeCycle,locationDeclared:D.locationDeclared,territorialReading:D.territorialReading||'',qualityIssues:D.qualityIssues,qualityAssuranceRules:D.qualityAssuranceRules,methodology:D.methodology,appliedRules:D.appliedRules,priorities:D.priorities,prioritizationMatrix:D.prioritizationMatrix,conclusions:D.conclusions,annexIndicators:D.annexIndicators,draftVersion:'3.0',draftCutoff:'2026-08-18'}}
async function seedInitialData(){await waitReady();requireRole('super_admin');const D=window.REPORT_DATA,geo=window.TERRITORY_MAP_DATA||[],batch=SDK.writeBatch(db);batch.set(SDK.doc(db,'rufeEvents',EVENT_ID),{name:'Emergencia sísmica San Pedro 2026',municipality:'San Pedro, Valle del Cauca',eventDate:'2026-08-10',active:true,updatedAt:SDK.serverTimestamp()},{merge:true});batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'drafts','current'),{...initialDraft(),seededAt:SDK.serverTimestamp(),updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'workflow','state'),{revision:1,reviewStatus:'draft',approvedRevision:null,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});(D.territories||[]).forEach(t=>{const g=geo.find(x=>x.sector===t.sector)||{};batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'territories',slug(t.sector)),{...t,lat:g.lat??null,lng:g.lng??null,precision:g.precision||'reference',reference:g.reference||'',source:g.source||'',updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true})});await batch.commit();return true}
const PUBLIC_KEYS=['meta','documentControl','executiveSummary','objective','scope','limitations','eventContext','regulatoryFramework','rufeRud','kpis','completeness','componentStatus','housing','housingInterpretation','priorityHousingNarrative','sexDistribution','lifeCycle','locationDeclared','territorialReading','qualityIssues','qualityAssuranceRules','methodology','appliedRules','priorities','prioritizationMatrix','conclusions','annexIndicators','visualReports'];
const TERRITORY_KEYS=['sector','families','people','noHab','destroyed','noState','empty','lat','lng','precision','reference','source'];
function stripTerritory(x){const o={};for(const k of TERRITORY_KEYS)if(x[k]!==undefined)o[k]=x[k];return o}
function itemCount(arr,label){return n((Array.isArray(arr)?arr:[]).find(x=>x.label===label)?.count)}
function updateByLabel(arr,label,patch){return (Array.isArray(arr)?arr:[]).map(x=>x.label===label?{...x,...patch}:x)}
function consistency(payload,totals){const k=payload.kpis||{},people=totals.people,families=totals.families;const sex=n(k.women)+n(k.men)+n(k.sexMissing);if(sex&&sex!==people)throw new Error(`La distribución por sexo suma ${sex}, pero el total territorial es ${people}.`);const life=(payload.lifeCycle||[]).reduce((s,x)=>s+n(x.count),0);if(life&&life!==people)throw new Error(`El ciclo de vida suma ${life}, pero el total territorial es ${people}.`);const loc=(payload.locationDeclared||[]).reduce((s,x)=>s+n(x.count),0);if(loc&&loc!==families)throw new Error(`La ubicación declarada suma ${loc}, pero el total de familias es ${families}.`);for(const [label,v] of [['Dirección',k.familiesWithAddress],['Teléfono',k.familiesWithPhone]])if(n(v)>families)throw new Error(`${label} no puede superar ${families} familias.`);if(n(k.uniquePeopleEstimated)>people)throw new Error('Las personas únicas estimadas no pueden superar los registros nominales.')}
function derive(draft,territories){const families=territories.reduce((s,t)=>s+n(t.families),0),people=territories.reduce((s,t)=>s+n(t.people),0),noHab=territories.reduce((s,t)=>s+n(t.noHab),0),destroyed=territories.reduce((s,t)=>s+n(t.destroyed),0),empty=territories.reduce((s,t)=>s+n(t.empty),0),without=territories.reduce((s,t)=>s+Math.max(0,n(t.noState)-n(t.empty)),0),withState=Math.max(0,families-without);const out={};for(const k of PUBLIC_KEYS)if(draft[k]!==undefined)out[k]=draft[k];out.kpis={...(out.kpis||{}),families,nominalPeople:people,territories:territories.length,familiesWithoutState:without,familiesWithState:withState,emptyPreEnumerated:empty};out.housing=updateByLabel(out.housing,'No habitable',{count:noHab,pct:families?noHab/families*100:0});out.housing=updateByLabel(out.housing,'Destruida',{count:destroyed,pct:families?destroyed/families*100:0});out.housing=updateByLabel(out.housing,'Sin clasificación registrada',{count:without,pct:families?without/families*100:0});const hab=itemCount(out.housing,'Habitable'),dam=itemCount(out.housing,'Averiada');out.housing=updateByLabel(out.housing,'Habitable',{count:hab,pct:families?hab/families*100:0});out.housing=updateByLabel(out.housing,'Averiada',{count:dam,pct:families?dam/families*100:0});if(Array.isArray(out.lifeCycle)){out.kpis.minors=itemCount(out.lifeCycle,'0–5')+itemCount(out.lifeCycle,'6–11')+itemCount(out.lifeCycle,'12–17');out.kpis.age60plus=itemCount(out.lifeCycle,'60 y más');out.lifeCycle=out.lifeCycle.map(x=>({...x,pct:people?n(x.count)/people*100:0}))}if(Array.isArray(out.locationDeclared)){out.kpis.familiesWithUrbanRural=itemCount(out.locationDeclared,'Rural')+itemCount(out.locationDeclared,'Urbana')}out.sexDistribution=[{label:'Femenino',count:n(out.kpis.women),pct:people?n(out.kpis.women)/people*100:0,tone:'magenta'},{label:'Masculino',count:n(out.kpis.men),pct:people?n(out.kpis.men)/people*100:0,tone:'blue'},{label:'Sin dato',count:n(out.kpis.sexMissing),pct:people?n(out.kpis.sexMissing)/people*100:0,tone:'gray'}];if(Array.isArray(out.completeness))out.completeness=out.completeness.map(x=>{let num=x.numerator,den=x.denominator;if(x.label==='Integrante nominal'){num=families;den=families}else if(x.label==='Teléfono'){num=n(out.kpis.familiesWithPhone);den=families}else if(x.label==='Dirección'){num=n(out.kpis.familiesWithAddress);den=families}else if(x.label==='Estado del inmueble'){num=withState;den=families}else if(x.label==='Fecha de nacimiento válida'){num=Math.max(0,people-n(out.kpis.invalidBirthDates));den=people}else if(x.label==='Sexo F/M válido'){num=Math.max(0,people-n(out.kpis.sexMissing));den=people}return {...x,numerator:num,denominator:den,value:den?num/den*100:0}});if(Array.isArray(out.qualityIssues)){const counts={'Núcleos numerados sin integrantes':empty,'Grupos de documento repetido':n(out.kpis.repeatedDocGroups),'Jefaturas con documento repetido':n(out.kpis.repeatedHeads),'Personas sin documento válido':n(out.kpis.peopleWithoutValidDocument),'Fecha de nacimiento inválida/ausente':n(out.kpis.invalidBirthDates),'Sexo sin dato F/M':n(out.kpis.sexMissing),'Familias nominales sin estado':without,'Registros con marcas múltiples':n(out.kpis.multipleHousingMarks),'Zona urbana/rural sin dato homologable':n(out.kpis.noZoneHomologable)};out.qualityIssues=out.qualityIssues.map(x=>counts[x.label]===undefined?x:{...x,count:counts[x.label]})}if(Array.isArray(out.componentStatus))out.componentStatus=out.componentStatus.map(x=>x.component==='Cobertura territorial'?{...x,progress:`${territories.length} hojas territoriales únicas analizadas`}:x.component==='Identificación familiar'?{...x,progress:`${families} núcleos con al menos una persona`,status:`En curso; ${empty} núcleos preenumerados están vacíos`}:x.component==='Caracterización individual'?{...x,progress:`${people.toLocaleString('es-CO')} registros nominales`}:x.component==='Clasificación del inmueble'?{...x,progress:`${withState} de ${families} familias`,status:`En curso; ${without} sin marca y ${n(out.kpis.multipleHousingMarks)} con marcas múltiples`}:x);if(Array.isArray(out.annexIndicators)){const map={'Territorios/sectores únicos':String(territories.length),'Registros con integrante nominal':String(families),'Núcleos numerados vacíos':String(empty),'Registros nominales':people.toLocaleString('es-CO'),'Únicas estimadas':n(out.kpis.uniquePeopleEstimated).toLocaleString('es-CO'),'Documentos repetidos — grupos':String(n(out.kpis.repeatedDocGroups)),'0–17 años':String(n(out.kpis.minors)),'60 años y más':String(n(out.kpis.age60plus)),'Familias nominales sin estado':String(without),'Marcas múltiples':String(n(out.kpis.multipleHousingMarks))};out.annexIndicators=out.annexIndicators.map(x=>map[x.indicator]!==undefined?{...x,value:map[x.indicator]}:x)}out.territories=territories.map(stripTerritory);consistency(out,{families,people});return out}
function validateTerritories(territories){for(const t of territories){const sector=clean(t.sector,160)||'Territorio sin nombre',families=n(t.families),people=n(t.people),noState=n(t.noState),empty=n(t.empty);if(people<families)throw new Error(`${sector}: personas (${people}) no puede ser menor que familias (${families}).`);if(noState<empty)throw new Error(`${sector}: “sin estado” (${noState}) debe incluir como mínimo los núcleos vacíos (${empty}).`);for(const [label,value] of [['No habitable',n(t.noHab)],['Destruida',n(t.destroyed)]])if(value>families)throw new Error(`${sector}: ${label} (${value}) no puede superar las familias (${families}).`);if(t.lat!=null&&(Number(t.lat)<-90||Number(t.lat)>90))throw new Error(`${sector}: latitud fuera de rango.`);if(t.lng!=null&&(Number(t.lng)<-180||Number(t.lng)>180))throw new Error(`${sector}: longitud fuera de rango.`)}}
async function loadEvent(){const [draftSnap,terrSnap,wfSnap]=await Promise.all([SDK.getDoc(SDK.doc(db,'rufeEvents',EVENT_ID,'drafts','current')),SDK.getDocs(SDK.collection(db,'rufeEvents',EVENT_ID,'territories')),SDK.getDoc(SDK.doc(db,'rufeEvents',EVENT_ID,'workflow','state'))]);if(!draftSnap.exists())throw new Error('No existe borrador inicial.');return {draft:draftSnap.data(),territories:terrSnap.docs.map(d=>d.data()).sort((a,b)=>clean(a.sector).localeCompare(clean(b.sector),'es')),workflow:wfSnap.exists()?wfSnap.data():{revision:0,reviewStatus:'draft'}}}
async function audit(action,detail='',meta={}){try{await SDK.addDoc(SDK.collection(db,'rufeAudit'),{actorUid:user?.uid||'',actorEmail:user?.email||profile?.email||'',actorRole:normalizeRole(profile?.role),action,eventId:EVENT_ID,detail,meta,createdAt:SDK.serverTimestamp()})}catch(e){console.warn('Auditoría no registrada:',e)}}
async function createUser(p={}){await waitReady();requireRole('admin','super_admin');const email=clean(p.email,240).toLowerCase(),password=String(p.password||''),displayName=clean(p.displayName,160),role=normalizeRole(p.role);if(!email||!email.includes('@'))throw new Error('Correo inválido.');if(password.length<8)throw new Error('La contraseña temporal debe tener al menos 8 caracteres.');if(!VALID_ROLES.includes(role))throw new Error('Rol inválido.');if(role==='super_admin'&&!hasRole('super_admin'))throw new Error('Solo Super Admin puede crear otro Super Admin.');let uid=email,createdInAuth=false,secondaryApp=null;try{secondaryApp=SDK.initializeApp(cfg,`rufe-create-${Date.now()}-${Math.random()}`);const secondaryAuth=SDK.getAuth(secondaryApp),cred=await SDK.createUserWithEmailAndPassword(secondaryAuth,email,password);uid=cred.user.uid;createdInAuth=true;if(displayName)await SDK.updateProfile(cred.user,{displayName});await SDK.signOut(secondaryAuth)}catch(e){if(e?.code==='auth/email-already-in-use'){uid=email}else{throw e}}finally{if(secondaryApp)try{await SDK.deleteApp(secondaryApp)}catch(_){}}
  await SDK.setDoc(SDK.doc(db,'users',uid),{uid:createdInAuth?uid:'',email,displayName,role,active:true,rufeAccess:true,createdAt:SDK.serverTimestamp(),createdBy:user.uid},{merge:true});await audit('user.create',createdInAuth?`Creó ${email}`:`Registró perfil RUFE para ${email}`,{uid,role,createdInAuth});return {uid,email,role,createdInAuth}}
async function manageUser(p={}){await waitReady();requireRole('admin','super_admin');const uid=clean(p.uid,180),action=clean(p.action,30);if(!uid)throw new Error('UID requerido.');if(uid===user.uid&&action==='active'&&p.active===false)throw new Error('No puedes desactivar tu propia cuenta.');const ref=SDK.doc(db,'users',uid),snap=await SDK.getDoc(ref);if(!snap.exists())throw new Error('Perfil no encontrado.');if(action==='role'){const role=normalizeRole(p.role);if(!VALID_ROLES.includes(role))throw new Error('Rol inválido.');if(role==='super_admin'&&!hasRole('super_admin'))throw new Error('Solo Super Admin puede asignar Super Admin.');if(uid===user.uid&&normalizeRole(profile?.role)==='super_admin'&&role!=='super_admin')throw new Error('No puedes retirar tu propio rol Super Admin.');await SDK.updateDoc(ref,{role,rufeAccess:true,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid});await audit('user.role',`Rol de ${uid}: ${role}`);return {ok:true}}if(action==='active'){const active=p.active===true;await SDK.updateDoc(ref,{active,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid});await audit('user.active',`${uid}: ${active}`);return {ok:true}}if(action==='delete'){if(uid===user.uid)throw new Error('No puedes eliminar tu propio perfil.');await SDK.deleteDoc(ref);await audit('user.delete',`Retiró perfil RUFE ${uid}`,{uid});return {ok:true}}throw new Error('Acción no soportada.')}
async function validateDraft(){await waitReady();requireRole('editor','validator','admin','super_admin');const {draft,territories,workflow}=await loadEvent();validateTerritories(territories);const payload=derive(draft,territories),revision=n(workflow.revision);await audit('workflow.validate','Comprobación de consistencia superada',{revision,families:payload.kpis?.families||0,people:payload.kpis?.nominalPeople||0});return {ok:true,revision,families:payload.kpis?.families||0,people:payload.kpis?.nominalPeople||0,territories:territories.length}}
async function submitForReview(note){await waitReady();requireRole('editor','admin','super_admin');const loaded=await loadEvent();validateTerritories(loaded.territories);derive(loaded.draft,loaded.territories);const ref=SDK.doc(db,'rufeEvents',EVENT_ID,'workflow','state'),w=loaded.workflow||{revision:1},revision=n(w.revision)||1;await SDK.setDoc(ref,{revision,reviewStatus:'in_review',submittedRevision:revision,approvedRevision:null,submittedAt:SDK.serverTimestamp(),submittedBy:user.uid,reviewNote:clean(note,2000),updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});await audit('workflow.submit','Borrador validado y enviado a revisión',{revision});return {ok:true,revision}}
async function reviewDraft(decision,note){await waitReady();requireRole('validator','admin','super_admin');const loaded=await loadEvent(),ref=SDK.doc(db,'rufeEvents',EVENT_ID,'workflow','state'),w=loaded.workflow||{},revision=n(w.revision);if(decision==='approve'){if(w.reviewStatus!=='in_review')throw new Error('El borrador no está en revisión.');if(n(w.submittedRevision)!==revision)throw new Error('El borrador cambió después de enviarse a revisión.');validateTerritories(loaded.territories);derive(loaded.draft,loaded.territories);await SDK.updateDoc(ref,{reviewStatus:'approved',approvedRevision:revision,approvedAt:SDK.serverTimestamp(),approvedBy:user.uid,approvalNote:clean(note,2000),updatedAt:SDK.serverTimestamp(),updatedBy:user.uid});await audit('workflow.approve','Revisión aprobada después del control de consistencia',{revision});return {ok:true,revision}}if(decision==='reject'){await SDK.updateDoc(ref,{reviewStatus:'changes',approvedRevision:null,rejectedAt:SDK.serverTimestamp(),rejectedBy:user.uid,rejectionNote:clean(note,2000),updatedAt:SDK.serverTimestamp(),updatedBy:user.uid});await audit('workflow.reject','Ajustes solicitados',{revision});return {ok:true,revision}}throw new Error('Decisión inválida.')}
async function publish(p={}){await waitReady();requireRole('super_admin');const version=clean(p.version,40)||'1.0',cutoffDate=clean(p.cutoffDate,20),changeNote=clean(p.changeNote,3000),{draft,territories,workflow}=await loadEvent(),revision=n(workflow.revision);validateTerritories(territories);if(workflow.reviewStatus!=='approved'||n(workflow.approvedRevision)!==revision)throw new Error('La revisión actual no está aprobada.');if(!cutoffDate)throw new Error('Fecha de corte requerida.');const payload=derive(draft,territories),mediaSnap=await SDK.getDocs(SDK.query(SDK.collection(db,'rufeEvents',EVENT_ID,'media'),SDK.where('includeInPublication','==',true))),media=mediaSnap.docs.map(d=>{const m=d.data();return {id:d.id,title:m.title||m.name,name:m.name,type:m.type||'annex',size:m.size||0,publicUrl:m.url||'',url:m.url||'',status:'published',publishedAt:new Date().toISOString()}});const currentRef=SDK.doc(db,'rufePublished',EVENT_ID),currentSnap=await SDK.getDoc(currentRef),current=currentSnap.exists()?currentSnap.data():{},summary={id:`${slug(version)}-${cutoffDate}`,version,cutoffDate,publishedAt:new Date().toISOString(),changeNote,kpis:payload.kpis,housing:payload.housing},history=[...(Array.isArray(current.releases)?current.releases:[]).filter(x=>x.id!==summary.id),summary].slice(-50),updates=[...(Array.isArray(current.updates)?current.updates:[]).filter(x=>x.id!==summary.id),{id:summary.id,date:cutoffDate,version,title:`Consolidado técnico v${version}`,note:changeNote||'Nuevo corte publicado.',status:'published'}].slice(-50),publication={status:'published',version,cutoffDate,publishedAt:summary.publishedAt,revision,publishedBy:user.email||user.uid},snapshot={...payload,media,releases:history,updates,publication},batch=SDK.writeBatch(db);batch.set(currentRef,snapshot);batch.set(SDK.doc(db,'rufePublished',EVENT_ID,'releases',summary.id),snapshot);batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'releases',summary.id),{...snapshot,createdAt:SDK.serverTimestamp()});batch.set(SDK.doc(db,'rufeEvents',EVENT_ID,'workflow','state'),{reviewStatus:'published',publishedRevision:revision,publishedAt:SDK.serverTimestamp(),publishedBy:user.uid,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid},{merge:true});await batch.commit();await audit('publication.publish',`Publicó v${version} · ${cutoffDate}`,{revision,media:media.length});return {ok:true,releaseId:summary.id,revision}}
async function manageMedia(p={}){await waitReady();requireRole('super_admin');const mediaId=clean(p.mediaId,180),action=clean(p.action,30),ref=SDK.doc(db,'rufeEvents',EVENT_ID,'media',mediaId),snap=await SDK.getDoc(ref);if(!snap.exists())throw new Error('Archivo no encontrado.');if(action==='publication'){await SDK.updateDoc(ref,{includeInPublication:p.includeInPublication===true,updatedAt:SDK.serverTimestamp(),updatedBy:user.uid});await audit('media.publication',mediaId,{include:p.includeInPublication===true});return {ok:true}}if(action==='delete'){const m=snap.data();if(m.path)try{await SDK.deleteObject(SDK.ref(storage,m.path))}catch(_){}await SDK.deleteDoc(ref);await audit('media.delete',mediaId);return {ok:true}}throw new Error('Acción no soportada.')}
async function listUsers(){await waitReady();requireRole('admin','super_admin');const s=await SDK.getDocs(SDK.collection(db,'users')),rows=s.docs.map(d=>({docId:d.id,...d.data(),role:normalizeRole(d.data().role)})),out=new Map();for(const x of rows){const key=x.uid||x.email||x.docId;if(!out.has(key)||x.docId===x.uid)out.set(key,x)}return [...out.values()].filter(x=>x.rufeAccess===true||permitted.includes(x.role)).sort((a,b)=>String(a.displayName||a.email).localeCompare(String(b.displayName||b.email),'es'))}
async function listAudit(){await waitReady();requireRole('super_admin');const s=await SDK.getDocs(SDK.query(SDK.collection(db,'rufeAudit'),SDK.orderBy('createdAt','desc'),SDK.limit(150)));return s.docs.map(d=>({id:d.id,...d.data()}))}
async function listMedia(){await waitReady();requireRole('editor','validator','admin','super_admin');const s=await SDK.getDocs(SDK.query(SDK.collection(db,'rufeEvents',EVENT_ID,'media'),SDK.orderBy('createdAt','desc'),SDK.limit(150)));return s.docs.map(d=>({id:d.id,...d.data()}))}
async function uploadMedia(file,{title,type='annex',includeInPublication=false,onProgress}={}){await waitReady();requireRole('super_admin');if(!file)throw new Error('Selecciona un archivo.');if(file.size>40*1024*1024)throw new Error('El archivo supera 40 MB.');const safe=file.name.replace(/[^a-zA-Z0-9._-]+/g,'-'),path=`rufe-drafts/${EVENT_ID}/${Date.now()}-${safe}`,task=SDK.uploadBytesResumable(SDK.ref(storage,path),file,{contentType:file.type||'application/octet-stream'});await new Promise((resolve,reject)=>task.on('state_changed',s=>onProgress?.(s.totalBytes?Math.round(s.bytesTransferred/s.totalBytes*100):0),reject,resolve));const url=await SDK.getDownloadURL(task.snapshot.ref),rec={title:title||file.name,type,name:file.name,path,url,size:file.size,contentType:file.type||'',includeInPublication:!!includeInPublication,createdAt:SDK.serverTimestamp(),createdBy:user.uid,status:'draft'},r=await SDK.addDoc(SDK.collection(db,'rufeEvents',EVENT_ID,'media'),rec);return {id:r.id,...rec}}
window.RUFE_FIREBASE={configured,EVENT_ID,init,loginGoogle,loginEmail,resetPassword,logout,listenPublic,getPublicRelease,listenDraft,listenTerritories,listenWorkflow,stopInternalListeners,saveTerritory,saveTerritoriesBulk,saveDraft,seedInitialData,createUser,manageUser,validateDraft,submitForReview,reviewDraft,publish,listUsers,listAudit,listMedia,uploadMedia,manageMedia,fallbackReport,normalizeRole,hasRole,get ready(){return ready},get user(){return user},get profile(){return profile}};
init();
